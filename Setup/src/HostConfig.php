<?
/**
 * sets the config for the host
 * may be database, logging, whatever
 *
 * rename the defaultConfigurateHost method into configurateHost and set the config values
 *
 * @author wolxXx
 * @version 1.0
 * @package application
 * @subpackage config
 */
class HostConfig extends AppConfig{
	/**
	 * (non-PHPdoc)
	 * @see CoreConfig::configureApplication()
	 */
	public function defaultConfigureHost(){}
}